name: ${project.name}
version: ${project.version}
description: ${project.description}
prefix: ${project.name}
author: LoadingByte
main: ${main.class}

commands:
    spectate:
        description: Lets you spectate another user
        aliases: [spec]
